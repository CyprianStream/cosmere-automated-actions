
export function progression(item, actor) {
    const target = game.user.targets.first();
    const actorProgressionRank = actor.system.skills.prg.rank;
    if(plantGrowth){
        // + @scalar.power.[skill].size;
    } else if(characterRegrowth){
        // + @scalar.power.[skill].die;
    }
}
